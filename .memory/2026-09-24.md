# 2026-09-24 — Session Log

## Epic 14 — Stories 14.0 + 14.1 implemented & audited (Prompt #3)

**Branch:** `dev` (no epic branch). Fixes **staged, not committed**.

### Implemented

- **14.0 — Schema, Migration, Server Caps, Privacy**
  - SQL: `docs/stories/sql-writeups/epic14-story0-qualification-schema.sql` — adds `qualification_questions.options jsonb`, remaps legacy `example`/text answers by waitlist+text, drops public SELECT policy on subscribers, `revoke select ... from anon`, `grant select ... to authenticated`.
  - `src/app/api/waitlist/route.ts` — cap checks (free:2 / pro:5 via `getTierLimits`), `normalizeQuestions()` (MC ≥2 non-empty → 400; free_text → `options: null`), GET returns `{id, text, type, options}` (no `required`).
  - Deleted `src/app/api/leaderboard/[subdomain]/route.ts` + test glob (AC7).
- **14.1 — Question Builder + Public Capture**
  - `src/app/onboarding/4a/page.tsx` — Free text / Multiple choice radiogroup, options editor (min 2, max 10), tier badge, cap upsell `triggerUpgrade("qual_question")`.
  - `components/public/email-capture-form.tsx` — id-keyed `answers[q.id]`, empty answers filtered before POST.
  - `src/app/api/subscribers/route.ts:524-546` — AC11 sanitization: drop unknown/empty `qual_answers` keys against question-id set.
  - `components/onboarding/live-preview.tsx` — MC radio-dot list + free-text bar parity; verbatim `What are you currently using?` at `:238`.

### Prompt #3 audit — 3 findings, all fixed

| #   | File                                                    | Problem                                                                                                                                                        | Fix                                                                                              |
| --- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 1   | `src/lib/milestones.ts`                                 | `createClient()` (anon) on public POST /api/subscribers — anon SELECT/UPDATE on subscribers fails after migration revoke → milestones silently skipped         | `createAdminClient()`                                                                            |
| 2   | `src/app/unsubscribe/page.tsx` + `resubscribe/page.tsx` | anon `.update().select()` — RETURNING needs SELECT privilege (revoked); HMAC verified before DB access                                                         | `createAdminClient()`                                                                            |
| 3   | `components/public/email-capture-form.tsx`              | MC fieldset hardcoded `bg-card border-border` (white on dark page); `(optional)`/footer hardcoded `text-muted-foreground` — mismatched LivePreview dark tokens | `inputBorder`/`inputBg`/`inputText`; `isDark ? text-dark-template-muted : text-muted-foreground` |

### Gates (all green, post-fix)

- `pnpm lint` → 0 errors, 5 pre-existing warnings
- `pnpm build` → success (`.next` deleted first)
- Full suite → **442 passed / 8 failed = exact baseline** (dashboard-archive 4, dashboard-subscriber-table 3, flaky billing 1)
- Targeted → 34/34

### Open / next

- **Deploy gate:** founder runs `epic14-story0-qualification-schema.sql` in Supabase SQL Editor before deploy.
- **Staged, uncommitted:** the 4 audit-fix files.
- **Next story:** 14.2 (settings question editor — COPY GAP on settings CTAs, stop-and-ask).
- Flagged, not fixed: `src/app/api/subscribers/[id]/route.ts` PATCH display_name anon `.select()` (possible AC8 read-path gap — verify in 14.4).
- Deferred: dashboard qualification API text-keyed (14.3), dead waitlist-page-content props + `dashboard/page.tsx:56` qual_answers select (14.4).

## Also today

- Epic 17 create-epic completed earlier today (planning only, not implemented) — see MEMORY.md Epic 17 section.
