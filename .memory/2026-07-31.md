# 2026-07-31 — Marketing Homepage Completion + Design Analysis

## Epic 3 Finalized

All stories (3.0–3.5) + extra work completed. Epic marked done in `docs/epics/epic-3-marketing-homepage.md`.

### What was built today

1. **Comparison Section** — White `bg-card` band, red ✗ SVG marks (not dots), green circled ✓ SVG marks (not dots), list spacing increased to `gap-4` for readability.

2. **Feature Grid** — 2×2 grid centered in `max-w-4xl`. Added small green SVG icons (person, flame, link, envelope) next to each feature title. Callout extracted to standalone ConfidenceSection.

3. **Confidence Section** — New file `components/marketing/confidence-section.tsx`. Standalone section, no hardcoded bg (warm ivory default). Text styled `text-body-lg text-accent` (subheader weight in brand green). "free" and "500" in bold. `border-y border-border` separates from adjacent sections.

4. **Pricing Section** — Base price format "Free – $0/month" (en dash). Green ✓ checkmarks (not dots). Both cards have CTA buttons ("Build it free — live in 4 mins →"). Cards aligned via `flex flex-col` + `flex-1` spacer. Pro card has `border-accent` and "Popular" badge.

5. **Navbar Scroll** — Border visible at top of page (scrollY=0), disappears on scroll. `useState` + `useEffect` with passive scroll listener.

6. **Footer** — Removed `bg-muted` (warm ivory). Bumped text from `text-caption` (12px) to `text-body` (16px). Logo increased from 36×27 to 48×36, gap reduced.

7. **Section Borders** — Removed `border-b` from ComparisonSection and DifferenceSection. Added `border-y` to ConfidenceSection.

### Marketing Homepage Final Section Order

Hero → ProblemSection → DifferenceSection → ComparisonSection → FeatureGrid → ConfidenceSection → PricingSection

### Tailwind v4 Gotcha

Unlayered CSS (plain CSS classes in `globals.css`) has **higher precedence** than Tailwind's layered utilities. When a plain CSS class sets a property (e.g., `.text-overline { text-transform: uppercase }`), Tailwind's `normal-case` utility cannot override it. Workaround: use inline styles or replace plain CSS classes with Tailwind utilities.

### Pricing Design Decision

Design showed CTA only in Pro card, but user overruled — both cards now have CTA for uniformity (best practice: every pricing tier should have a clear call-to-action).

---

## Epic 4 Design Analysis Complete

**Time:** Evening session
**File:** `docs/design/design-analysis.md`

### What was accomplished

1. **Read all 10 design SVGs** — Steps 1–5 (including Step 4 variant, Step 2 variants), Success screen
2. **Extracted PRD sections 6.6–6.12** — All REQ IDs (REQ-6.6.1 through REQ-6.12.2+)
3. **Documented per-screen analysis** — Layout, typography, colors, tokens, form elements, buttons, spacing
4. **Cross-referenced PRD vs designs** — Match status for each REQ
5. **Cross-referenced designs vs implementation** — Current state of onboarding pages
6. **Web research** — Multi-step onboarding UX, template selectors, color pickers, qualification question builders
7. **Confidence check** — 98% confidence, all analysis complete

### Key design findings

1. **Layout split:** Steps 1–3 = two-pane (566px left + browser mockup). Steps 4, 5, Success = centered (no right pane).
2. **Button pattern:** Arrow-only (→) for Steps 1–4. Text+arrow for Step 5.
3. **Progress dots:** 5 dots, same color for completed and current (`#0F7A5E`).
4. **Template cards:** Selected state uses 2px accent border.
5. **Submit button:** 458×59px, rx=9.59, `#0F7A5E` fill.
6. **Success screen:** No progress dots, terminal state.

### Next step

Restructure Epic 4 document to match design reality before executing stories. User expects current stories don't align with designs.

---

## Epic 4 Document Restructured

**Time:** Late evening session
**File:** `docs/epics/epic-4-onboarding-wizard.md`

### What was accomplished

1. **Analyzed current Epic 4 stories** against design analysis findings
2. **Restructured all 8 stories** (4.0–4.7) to align with design reality
3. **Added design specs** to each story — exact dimensions, colors, tokens from SVG analysis
4. **Updated layout patterns** — Steps 1–3 = two-pane, Steps 4/4a/5/Success = centered
5. **Updated button patterns** — Steps 1–4 = arrow-only (→), Step 5 = text+arrow ("Launch my waitlist" + →)
6. **Updated progress dots** — same color for completed and current (no checkmarks)
7. **Updated Story 4.0** — added layout switching, progress dot updates, success screen handling
8. **Added story dependency graph** and implementation order
9. **Ran lint + build** — zero errors, all routes compile

### Key changes from original epic

| Aspect         | Original                     | Restructured                                     |
| -------------- | ---------------------------- | ------------------------------------------------ |
| Layout         | Fixed two-pane for all steps | Two-pane (1–3) + centered (4, 5, Success)        |
| Progress dots  | Checkmarks for completed     | Same accent color for completed + current        |
| Step 4 layout  | Two-pane                     | Centered (no right pane)                         |
| Step 5 layout  | Two-pane                     | Centered (no right pane)                         |
| Success layout | Two-pane                     | Centered, no progress dots                       |
| Step 5 button  | Same as others               | Wider (644px) with text "Launch my waitlist" + → |
| Design specs   | None                         | Exact dimensions, colors, tokens per screen      |

### Confidence

100% — Epic document now fully aligned with design analysis. All stories have design specs, acceptance criteria, and dev notes that match the high-fidelity SVGs.

---

## Epic 4 Alignment Check Complete

**Time:** Night session
**Prompt:** #6 (align-epic)

### What was accomplished

1. **Phase 1: Document Analysis** — Extracted all 8 story IDs, 80+ ACs, 20+ file paths, 10 design references, dependency relationships
2. **Phase 2: Implementation Scan** — Scanned codebase for all referenced files and components
3. **Phase 3: Cross-Reference Check** — Verified each AC has corresponding implementation
4. **Phase 4: Discrepancy Report** — Categorized findings (status mismatches, missing impl, scope drift)
5. **Phase 5: Documentation Updates** — Verified Dev Notes already have accurate status annotations
6. **Phase 6: Report** — Delivered alignment report

### Key findings

- **Status match:** All stories correctly marked as "ready"
- **Implementation:** All 80+ ACs have 0% implementation — all step pages are bare placeholders
- **Missing files:** API routes, onboarding context, all step page implementations
- **Layout issue:** Fixed two-pane layout needs switching logic for centered steps
- **Component availability:** All 13 referenced components exist and are properly implemented
- **Dev Notes accuracy:** All status annotations are correct and precise
- **Scope drift:** None detected — no code exists beyond what stories justify

### Confidence

100% — Epic document is accurately aligned with current implementation. Ready to execute stories.

---

## Story 4.0 Executed

**Time:** Night session
**Story:** API Routes + Shared Form Context + Layout Switching

### Files Created

1. **`src/app/api/waitlist/route.ts`** — POST + PATCH handlers
   - POST: Creates waitlist record with founder's user ID, returns created record ID
   - PATCH: Updates waitlist by ID, accepts partial fields, validates founder ownership
   - Auth check: Uses `supabase.auth.getUser()`, returns 401 if unauthorized

2. **`src/app/api/waitlist/check-slug/route.ts`** — GET handler
   - Checks slug availability against `waitlists.subdomain` table
   - Rejects reserved words: api, www, app, admin, dashboard, onboarding, signin, signup, verify-email
   - Returns `{ available: boolean }`

3. **`src/app/onboarding/context.tsx`** — OnboardingFormContext
   - Client Component with `useState` for all 14 form fields
   - Provides `updateField(key, value)`, `setWaitlistId(id)`, `setLoading(loading)`
   - Default `tier` to `"free"`
   - Exports `OnboardingFormProvider` and `useOnboardingForm` hook

4. **`components/onboarding/sticky-cta.tsx`** — Sticky CTA wrapper
   - `sticky bottom-0` on mobile, `md:static` on desktop
   - Used by step pages to keep submit buttons visible

### Files Modified

5. **`src/app/onboarding/layout.tsx`** — Complete rewrite
   - Wrapped in `OnboardingFormProvider`
   - Layout switching: two-pane for Steps 1–3, centered for Steps 4, 4a, 5, Success
   - LivePreview wired into right pane for two-pane routes
   - Progress dots: touch targets (`min-w-[44px] min-h-[44px]`), same color for completed/current (`#0F7A5E`)
   - Progress dots hidden on Success screen
   - Back link hidden on Success screen

### Verification

- `pnpm lint` — zero errors ✓
- `pnpm build` — zero errors, all routes compile ✓
- New routes: `ƒ /api/waitlist`, `ƒ /api/waitlist/check-slug` ✓

### AC Coverage

| AC   | Status | Implementation                              |
| ---- | ------ | ------------------------------------------- |
| AC1  | ✅     | POST /api/waitlist route                    |
| AC2  | ✅     | PATCH /api/waitlist route                   |
| AC3  | ✅     | GET /api/waitlist/check-slug route          |
| AC4  | ✅     | Reserved words list in check-slug           |
| AC5  | ✅     | OnboardingFormContext with all 14 fields    |
| AC6  | ✅     | updateField + setWaitlistId functions       |
| AC7  | ✅     | LivePreview wired in two-pane layout        |
| AC8  | ✅     | Centered layout for Steps 4, 4a, 5, Success |
| AC9  | ✅     | min-w-[44px] min-h-[44px] on dot links      |
| AC10 | ✅     | step.num <= currentStep ? accent : grey     |
| AC11 | ✅     | StickyCTA component created                 |
| AC12 | ✅     | loading/setLoading in context               |
| AC13 | ✅     | Progress dots hidden on /onboarding/success |
| AC14 | ✅     | Lint + build pass                           |

### Confidence

100% — All ACs implemented, lint+build pass, all routes compile.
