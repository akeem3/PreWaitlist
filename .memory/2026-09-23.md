# 2026-09-23 — Session Log

## Shipped

| Commit                                  | Summary                                                                          |
| --------------------------------------- | -------------------------------------------------------------------------------- |
| `2cf62be` (prior day, documented today) | Paddle checkout diagnostics, success redirect, webhook logging                   |
| `211085f`                               | Dashboard tier refresh — shell-owned state, post-upgrade polling, cross-tab sync |

Both pushed to `origin/dev` and fast-forwarded to `origin/main`. Working tree clean except untracked `.sisyphus/`.

## Dashboard Tier Refresh (`211085f`)

**Why:** After upgrade, sidebar stayed locked until full reload — layout tier prop never refreshed client-side.

**Mechanism:**

- `src/app/dashboard/shell.tsx` — client `tier` state seeded from server prop; poll `GET /api/profile` 2s × 60s cap; triggers: `paddle-checkout-opened`, `tier-changed`, mount-only `?upgraded=1` (stripped via `history.replaceState`), `BroadcastChannel("prewaitlist-tier")`; `router.refresh()` on change; context gains `refreshTier` + `useRefreshTier()`.
- `src/app/dashboard/settings/billing/client.tsx` — context is tier source of truth (`contextTier || profile?.tier || "free"`); mount re-fetch 0/2/5s dispatches `tier-changed` if tier differs; checkout/`?upgraded=1` local poll retained.
- Tests: new `dashboard-tier-refresh.test.tsx` (10); updated `dashboard-context.test.tsx`.

**Not changed:** `cancellation-flow.tsx` (external portal cancel; billing mount re-fetch covers race).

**Quality:** lint 0 errors / 5 pre-existing warnings; full suite 443 pass / 7 fail = known baseline (`dashboard-archive` 4, `dashboard-subscriber-table` 3).

## Open — Paddle Webhook (user action required)

1. In Paddle sandbox (`sandbox-vendors.paddle.com`), change webhook destination from apex `https://prewaitlist.com/...` to **`https://www.prewaitlist.com/api/webhooks/paddle`** (apex causes 308; Paddle does not follow redirects).
2. Re-send a failed event or run a new test payment (success card `4000 0566 5566 5556`).
3. Confirm Vercel logs: `Paddle webhook received` → `founder_profiles.tier = 'pro'` → sidebar unlocks within ~2s via tier refresh.

Secret suffix `…ySB9` already matches `PADDLE_WEBHOOK_SECRET`. Vercel env vars still unverified (no CLI).

## Artifacts

- Plan: `.sisyphus/plans/dashboard-tier-refresh.md` (untracked, intentional)
