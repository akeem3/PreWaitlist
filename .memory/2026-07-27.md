# 2026-07-27 — Session Log

## Session 2 (evening)

- Full docs scan completed: PRD, product vision, design SVGs, all config files
- Sprint 1 scope confirmed: 14 screens, 5 tables, ~6 epics estimated
- 17 high-fidelity SVGs mapped to PRD screens
- Epic 0 pushed to GitHub, Vercel auto-deploy triggered
- Memory updated with Sprint 1 scope and design references

## Session 3 (night)

- **Design System v2.0 completed**
- globals.css expanded from 59 lines → 225 lines → ~300 lines
- **Tokens added:**
  - Semantic states (success, warning, error, info)
  - Type scale (9-level system based on GetWaitlist)
  - Line heights (body + heading variants)
  - Letter spacing (tighter to wider)
  - Font weights (light, normal, medium, semibold, bold)
  - Z-index scale (base through tooltip)
  - Motion tokens (duration, easing)
  - Component tokens (button, input, card, badge, toggle, select)
  - Form field states (focus, error, disabled)
- **Typography presets added:** display, display-lg, h1-h4, body-lg, body, body-sm, caption, fine-print, label, overline, code
- **Research completed:**
  - GetWaitlist typography system analyzed (9-level tokens, ~1.16 ratio)
  - Best practices from Lollypop, FontFYI, Mavik Labs, Inkbot Design
  - Tailwind CSS v4 typography conventions
- **Key decisions:**
  - Base size: 16px (WCAG standard, browser default)
  - Scale ratio: ~1.16 (moderate humanist, tighter than Major Third)
  - Inter font retained (optimized for screen, variable weight)
  - rem units for accessibility
  - Unitless line-heights for fluid scaling
- Lint + build passed

## What happened

- User confirmed all manual external-service tasks are done except memsearch (Docker):
  - Supabase project + Google OAuth ✅
  - Paddle sandbox account + keys ✅
  - Vercel project + domain + nameservers ✅
- `.env.local` fully populated with Supabase, Google OAuth, and Paddle keys
- MEMORY.md updated to reflect current state
- User requested rewritten steps for memsearch (Docker setup)
- User requested Resend explanation + setup steps

## Unblocked stories ready to execute

- **0.3 (Supabase):** Credentials ready — needs client modules, schema DDL, RLS policies
- **0.5 (Paddle):** Keys in .env.local — needs verification/setup-only tasks
- **0.6 (Vercel):** Domain added, nameservers set — needs wildcard domain + proxy.ts

## Blocked

- **0.4 (Resend):** Account not created, no API key
- **0.7 (memsearch):** Docker install in progress
- **0.10 (smoke test):** Depends on 0.3 + 0.6

## Decisions

- None today — awaiting user direction on which story to run next.

## Story completions today

- **Story 0.7 (memsearch):** DONE — Milvus running via docker-compose, 15 chunks indexed
- **Story 0.4 (Resend):** DONE — API key in .env.local, setup only
- **Story 0.3 (Supabase):** DONE — client modules, schema DDL, RLS policies all in place, SQL run in dashboard

## Next

- Story 0.6 — proxy.ts subdomain routing
- Story 0.5 — Paddle verification
- Story 0.10 — smoke test
